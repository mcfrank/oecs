baseURL = "https://example.github.io/oecs-static/"
languageCode = "en-us"
title = "Open Encyclopedia of Cognitive Science"

# Site-level defaults (shown on article pages when not in front matter)
[params]
  editors_in_chief = ["Michael C. Frank", "Asifa Majid"]
# Override iframe URL: semantic_network_iframe_url = "https://..."

# Required for GitHub Actions (Hugo docs)
[caches]
[caches.images]
  dir = ":cacheDir/images"
